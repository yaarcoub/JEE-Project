spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://mysql:3306/ensam_bibliotheque?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root

cors:
  allowed-origins: http://frontend:5173,http://localhost:5173
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS

server:
  port: 8080

